[package]
name = "symposium-artifacts"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
description = "Artifact assembly for Symposium"

[dependencies]
minijinja = "2"
serde = { version = "1", features = ["derive"] }
toml = "0.8"
